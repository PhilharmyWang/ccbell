#!/bin/sh
# scripts/pre-commit — Run before each commit to check for secrets.
# Install: cp scripts/pre-commit .git/hooks/pre-commit

exec python "$(git rev-parse --show-toplevel)/scripts/check_secrets.py"
